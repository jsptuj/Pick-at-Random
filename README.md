# Pick at Random

Console application that reads a CSV file with dynamic headers and produces a randomly ordered, digitally signed PDF report in Slovenian.

Status: **Stage 0 — Bootstrap.** Implementation begins in Stage 1 (see `CLAUDE.md`).
