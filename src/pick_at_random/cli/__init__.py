"""CLI layer: composition root.

The ``main`` function lives in :mod:`pick_at_random.cli.main` and is
intentionally not re-exported here — re-exporting causes a
``RuntimeWarning`` when the module is invoked via ``python -m
pick_at_random.cli.main``.
"""
